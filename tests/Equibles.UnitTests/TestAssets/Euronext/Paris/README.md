# Paris directory captures

- `directory.html` is the unchanged directory page captured on 2026-09-15 from `https://live.euronext.com/en/markets/paris/equities/list`; its Drupal settings name the gateway `/en/product_directory/data/stocks-paris?mics=ALXP%2CXMLI%2CXPAR%2CXPMC`, the same shape as Lisbon with the market's own venue set.
- `equities.json` is the POST response from that gateway captured on 2026-09-15 (form `iDisplayStart=0`, `iDisplayLength=100`, `sSortDir_0=asc`, `sSortField=name`, `args[display_datapoints]=name,isin,symbol,market,lastPrice,precentDayChange,lastTradeTime`), cut down to five verbatim rows with `iTotalRecords` and `iTotalDisplayRecords` set to 5: ABO GROUP (market cell `XBRU, XPAR`, product link `BE0974278104-XBRU`, a Brussels-homed cross-listing), ACCOR on XPAR and ACCOR USD on XPMC (one ISIN quoted on two Paris venues), AIR FRANCE -KLM (`XPAR, XAMS`, linked `-XPAR`) and AIR LIQUIDE (`XPAR`). The full page held 100 of 773 rows, 17 of them multi-venue.
- `totalenergies.html` is the unchanged product page captured on 2026-09-15 from `https://live.euronext.com/en/product/equities/FR0000120271-XPAR`; its `custom.instrument` record states `type` `STOCK`, `symbol` `TTE`, `mic` `XPAR` and `issuer_code` `002816`.
